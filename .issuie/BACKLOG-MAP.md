# Backlog map — LinzLos/FLOWIE

Generated by issuie from a **FLOWIE v2.6 repo review + positioning shift** (2026-06-14).
**✅ 6 issues created (#1–#6) + pinned tracking issue #7.**

> Positioning: **FLOWIE is input/output tool-agnostic** — it takes a screenshot,
> code, a live URL, or a live **MCP connection** from any source (Figma-MCP, a
> browser/DOM MCP, …) and critiques the flow. No hardcoded product list. Reviewed on
> its own terms (FLOWIE's `.txt` is a real usage rendering — *not* SYNTHIE's bug).

## Two independent tracks
- **Cases track** (`cases/`): #1 → #2 — both touch `cases/README.md`, so sequential.
- **Script track** (`scripts/versions/v2.7/*`): #3 → #4 → #5 serialize on the script,
  then **#6 cuts v2.7**.
- Tracks are disjoint (`cases/` vs `scripts/` + `manifest`) → **run in parallel**.

## Issues (#NN — GitHub)

| # | Type · Pri | Title | Touches | Source (finding / direction) |
|---|---|---|---|---|
| #1 | chore · **P1** | Regression cases for the 4 untrapped invariants | `cases/002..005-*`, `cases/README.md` | 5 invariants, only `indexed-order` (001) has a case |
| #2 | chore · **P1** | Real screenshot/code extraction fixture | `cases/006-*`, `cases/README.md` | 001 feeds prose; v2.6 extraction headline untested |
| #3 | feat · **P1** | Make FLOWIE input/output-agnostic + MCP-capable | `scripts/versions/v2.7/*` *(shared)*, `README.md` | Tool-stack shift; input-agnostic + MCP positioning; drop V0/Figma/Framer + back-of-house lock |
| #4 | bug · **P1** | Fix `.xml`/`.txt` drift + parity guard | `scripts/versions/v2.7/*` *(shared)* | v2.6 `.xml` missing `<heuristics_reference>` + versionhistory that `.txt` has |
| #5 | feat · P2 | Role-based critique lenses (navigator / friction / a11y) | `scripts/versions/v2.7/*` *(shared)* | README lists as roadmap; absent from both files |
| #6 | chore · **P1** | Cut v2.7 — manifest + CHANGELOG + version dir | `manifest.json`, `CHANGELOG.md`, `README.md`, `scripts/versions/v2.7/` | release that ships #3–#5, in sync |

**Tracking (pinned):** #7 — 📋 FLOWIE v2.7 — roadmap & build order (start here)

## Dependency graph
```
#1 ─▶ #2                    (share cases/README)
#3 ─┐
#4 ─┼─▶ #6                  (v2.7 ships the script changes, in sync)
#5 ─┘
cases track ∥ script track  (disjoint files)
```

## Hand-offs (cross-owner)
None — single maintainer (Lindsay). All internal sequencing.

## Needs scoping
None.

## Roadmap read (what to prioritize)
1. **#3 is the headline** — the agnostic + MCP repositioning (drop product lock-in).
2. **#4** fixes the drift that hid the heuristics from the primary `.xml`; bake a parity
   guard so it can't recur (the single-source-of-truth idea folds in here).
3. **#1/#2** harden the invariant test net (#2 finally exercises the extraction path).
4. **#5** folds into the v2.7 script branch; **#6** cuts the release.
